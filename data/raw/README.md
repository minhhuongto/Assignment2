# data/raw

The seven source series exactly as downloaded, written by `R/01_collect_data.R`.
Nothing here is filtered or edited, so every cleaning decision can be revisited
without downloading again.

The files themselves are not committed. Run the pipeline to recreate them.
